{
  "name": "ue-blueprint-to-md",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Paste Unreal Engine Blueprint copy/paste text, get an annotated ASCII flow diagram and a documentation-ready markdown export. Fully client-side, no backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "vite": "^8.0.13",
    "vitest": "^2.1.9"
  }
}
