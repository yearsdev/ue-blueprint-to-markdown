{
  // Static-asset-only Worker for the UE Blueprint → Markdown app.
  // `npm run build` emits dist/, which Cloudflare serves directly.
  "name": "ue-blueprint-to-markdown",
  "compatibility_date": "2026-06-17",
  "assets": {
    "directory": "./dist"
  }
}
